/* ADMIN API — Manage users/properties (abhi dummy hai) */

async function getAdminStatsApi() {
  console.warn('adminApi.getAdminStatsApi: backend not connected yet');
  return { success: false, data: null };
}

async function approvePropertyApi(id) {
  console.warn('adminApi.approvePropertyApi: backend not connected yet');
  return { success: false };
}

async function rejectPropertyApi(id) {
  console.warn('adminApi.rejectPropertyApi: backend not connected yet');
  return { success: false };
}
